import { describe, expect, it } from 'vitest';
import { useCountryDashboard } from './useCountryDashboard';

describe('useCountryDashboard', () => {
  it('is defined', () => {
    expect(useCountryDashboard).toBeDefined();
  });
});

